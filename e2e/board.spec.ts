// import { test, expect } from "@playwright/test";

// test("issues are drag and droppable", async ({ page }) => {
//   await page.goto("/board");

//   const draggable = await page.getByRole('button').boundingBox();

//   const droppable = await page.locator('#droppableContainer').boundingBox();

//   await page.dragAndDrop(draggable,droppable);


//   await page.waitForTimeout(5000);
// });
